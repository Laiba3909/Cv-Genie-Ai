[project]
name = "multiagent-st"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.7"

dependencies = [
    "streamlit>=1.33.0",
    "openai>=1.0.0",
    "python-dotenv>=0.9.0"
]
